repos:
  - repo: local
    hooks:
      - id: prettier-check
        name: Prettier check
        entry: npm run check
        language: system
        pass_filenames: false
        stages: [pre-commit]
